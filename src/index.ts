#!/usr/bin/env node

import graphqlToTS from "graphql-to-ts";
import path = require("path");
const argv = require("minimist")(process.argv.slice(2));

async function main() {
  const cmd = process.argv[2];
}

main();
